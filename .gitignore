# Secrets — NEVER commit the monitor SSH keypair or GitHub read token
monitor_key
monitor_key.pub
github_token

# Runtime state and rendered output (regenerated on Jarvis)
last_run.json
history.jsonl
report.md
reports/
web/
__pycache__/
*.pyc

# Standing context is a copy of the (already-tracked) vault pentest doc
context/

# Console: generated retrieval index + conversation log (Jarvis-local)
retrieval-index.json
context/conversations.jsonl
